<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.blindproject.blind.dao.CareerDao">
	
	<insert id="insertCareer" parameterType="com.blindproject.blind.entity.Career">
		insert into Career(preCompany, position, strDate, endDate, task, resumeId )
		values(#{preCompany}, #{position}, #{strDate}, #{endDate}, #{task}, #{resumeId})
	</insert>
	
	
</mapper>