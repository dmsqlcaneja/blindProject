<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.blindproject.blind.dao.TypeDao">
	
	<select id="get" resultType="com.blindproject.blind.entity.Type">
		select * from type
		where id = #{param1}
	</select>
	
	<select id="getList" resultType="com.blindproject.blind.entity.Type">
		select * from type
<!-- 		where id = #{param1} -->
	</select>
	
</mapper>