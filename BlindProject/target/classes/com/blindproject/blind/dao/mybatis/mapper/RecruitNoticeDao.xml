<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blindproject.blind.dao.RecruitNoticeDao">
	
	<insert id="insert" parameterType="com.blindproject.blind.entity.RecruitNotice">
		insert into Recruit_Notice as RecruitNotice(title, Contents, reg_date, writer, str_date, end_date)
		values(#{title}, #{contents}, sysdate(), #{writer}, #{strDate}, #{endDate});
	</insert>
	
	<select id="get" resultType="com.blindproject.blind.entity.RecruitNotice">
		select
			id,
			title,
			contents,
			reg_date as regDate,
			writer 
		from Recruit_Notice as RecruitNotice
		where id = #{param1}
	</select>
	
	<select id="getList" resultType="com.blindproject.blind.entity.RecruitNotice">
		select 
			 id,
			 title,
			 contents,
			 reg_date as regDate,
			 writer,
			 str_date as strDate,
			 end_date as endDate
		from Recruit_Notice as RecruitNotice
	</select>
	
</mapper>