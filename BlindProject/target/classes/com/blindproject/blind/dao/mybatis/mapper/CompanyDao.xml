<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.blindproject.blind.dao.CompanyDao">
	
	<select id="get" resultType="com.blindproject.blind.entity.Company">
		select * from Company
		where id = #{param1}
	</select>
	
	<select id="getCompanyList" resultType="com.blindproject.blind.entity.Company">
		select * from Company
<!-- 		where id = #{param1} -->
	</select>
	
</mapper>